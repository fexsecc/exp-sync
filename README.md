## exp-sync
Tool designed for the pwncollege environment, where the main vulnerable machine is landlocked and can only be accessed through ssh.
## Usage
```bash
exp-sync.py <FILE> &
nohup exp-sync.py <FILE> >/dev/null 2>&1 &
```
